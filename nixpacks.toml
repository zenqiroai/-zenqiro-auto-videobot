[phases.setup]
nixPkgs = ["chromium", "chromedriver", "glibc", "nss", "atk", "cups", "dbus", "libdrm", "libxkbcommon", "libxcomposite", "libxdamage", "libxfixes", "libxrandr", "mesa", "pango", "cairo", "gdk-pixbuf"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"